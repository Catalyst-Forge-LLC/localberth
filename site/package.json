{
	"name": "localberth-site",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "filepress dev",
		"build": "filepress build",
		"preview": "filepress preview",
		"check": "filepress check",
		"ship": "pnpm build && wrangler pages deploy build --project-name=localberth",
		"deploy": "pnpm ship"
	},
	"devDependencies": {
		"getfilepress": "link:../../filepress",
		"wrangler": "^4.28.0"
	},
	"pnpm": {
		"onlyBuiltDependencies": ["esbuild", "getfilepress", "workerd"]
	}
}
